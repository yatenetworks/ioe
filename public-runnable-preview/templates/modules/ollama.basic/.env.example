# Ollama basic template (testing preview)
#
# No API keys or passwords are required for this draft template.
# The Ollama API is not authenticated by default in this draft setup.
#
# Model and runtime data are stored under the module app directory:
#   <IOE_DATA_DIR>/apps/ollama.basic/data/
