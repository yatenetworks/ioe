# Flowise basic template (testing preview)
#
# No repository secrets are required for this draft template.
# API keys or credentials entered in the Flowise UI are user-managed;
# do not commit them to git.
#
# Persistent data is stored under the module app directory:
#   <IOE_DATA_DIR>/apps/flowise.basic/data/
