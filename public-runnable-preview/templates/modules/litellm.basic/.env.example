# LiteLLM basic template (testing preview)
#
# Copy this file to .env in the installed module app directory for local testing.
# Never commit .env or real provider keys to git.
#
# Placeholders only — replace with your own local values.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
AZURE_API_KEY=
AZURE_API_BASE=

# Local testing placeholder for proxy master key (not for public use).
LITELLM_MASTER_KEY=change-me-local-testing-only
