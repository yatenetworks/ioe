# Qdrant basic template (testing preview)
#
# No API keys or passwords are required for this draft template.
# Authentication is not enabled by default.
#
# Persistent data is stored under the module app directory:
#   <IOE_DATA_DIR>/apps/qdrant.basic/storage/
