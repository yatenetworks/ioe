services:
  hello-basic:
    image: nginx:alpine
    container_name: ioe-hello-basic
    restart: unless-stopped
    ports:
      - "127.0.0.1:18080:80"
    labels:
      ioe.module_id: "hello.basic"
